interface:
  display_name: "Configure Allure Agent Workflow"
  short_description: "Add AI-agent workflow guidance for Allure test work"
  default_prompt: "Use $allure-configure-agent-workflow to add Allure agent workflow guidance to this project."

policy:
  allow_implicit_invocation: true
